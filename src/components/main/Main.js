import classes from "./styles.module.css";

const Main = () => (
  <main className={classes.root}>
    <h1 className={classes.heading}>Content</h1>
    <p className={classes.paragraph}>This is some content</p>
  </main>
);

export default Main;
